import { defineConfig } from "prettier";

export default defineConfig({
  trailingComma: "none",
  printWidth: 80,
  semi: true,
  singleQuote: true,
  arrowParens: "always",
  tabWidth: 2,
  bracketSpacing: true,
  spaceBeforeFunctionParen: false,
});
